apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: t
spec:
  taskRef:
    name: sonar
  workspaces:
  - name: maven-settings
    configmap:
      name: mvn-settings
  - name: maven-local-repository
    persistentVolumeClaim:
      claimName: pipeline-mvn
  - name: source
    persistentVolumeClaim:
      claimName: pipeline-src
    subPath: test1234567/api-gateway
