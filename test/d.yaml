apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: ftd
spec:
  taskSpec:
    steps:
    - name: s
      image: 10.1.40.43:5000/alpine/git:1.0.17
      command:
      - sleep
      - 1d
      volumeMounts:
      - name: t
        mountPath: /tekton/creds-secrets/pipeline-git-ssh-key
    volumes:
    - name: t
      secret:
       secretName: pipeline-git-ssh-key
       
