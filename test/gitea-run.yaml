apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: gitea
spec:
  params:
    - name: cmd
      value: createIssue
    - name: args
      value: |
        root/test-project 1 "Hello Word!"
        root/test-project 2 "Hello Word!"
        root/test-project 3 "Hello Word!"
        root/test-project 4 "Hello Word!"
        root/test-project 5 "Hello Word!"
        root/test-project 6 "Hello Word!"
  taskRef:
    name: gitea
  workspaces:
  - name: token
    secret:
      secretName: gitea-user-token
    subPath: token 
