apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: env-pipeline
spec:
  serviceAccountName: trigger
  triggers:
  - name: pipeline
    bindings:
    - ref: parse-env-event
    template:
      name: env-pipeline
---

apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerBinding
metadata:
  name: parse-env-event
spec:
  params:
  - name: env
    value: $(header.additional)
  - name: repo-url
    value: $(body.TitleLink)
  - name: repo-ref
    value: $(body.Event.metadata.commits[0].revision)
---

apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerTemplate
metadata:
  name: env-pipeline
spec:
  params:
  - name: env
  - name: repo-url
  - name: repo-ref

  resourcetemplates:
  - apiVersion: tekton.dev/v1beta1
    kind: PipelineRun
    metadata:
      name: env-pipeline-$(uid)
    spec:
      pipelineRef:
        name: env-pipeline-$(params.env)
      params:
      - name: env
        value: $(params.env)
      - name: repo-url
        value: $(params.repo-url)
      - name: repo-ref
        value: $(params.repo-ref)
