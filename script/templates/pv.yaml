apiVersion: v1
kind: PersistentVolume
metadata:
  name: pipeline-all-workspaces-pv-${PV_SUFFIX}
  labels:
    purpose: pipeline-all-workspaces
spec:
  hostPath: 
    path: /tmp/path/${PV_SUFFIX}
    type: DirectoryOrCreate
  capacity:
    storage: 8Gi
  accessModes:
  - ReadWriteMany
---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: pipeline-mvn-repo-pv-${PV_SUFFIX}
  labels:
    purpose: pipeline-mvn-repo
spec:
  hostPath: 
    path: /home/vagrant/pv/mvn
    type: DirectoryOrCreate
  capacity:
    storage: 8Gi
  accessModes:
  - ReadWriteMany
