taskRef:
  kind: ClusterTask
  name: image-build
params:
- name: dest-prefix
  value: inner-docker-registry:5000/$(params.repo-full-name)/
- name: dest-suffix
  value: ":$(params.repo-ref)"
- name: insecure-registry
  value: inner-docker-registry:5000
workspaces:
- name: source
  workspace: pipeline
  subPath: temporary/project-repo
